# 🇹🇭📄 PasitDev OCR App (Thai OCR with Python & Web)

แอปพลิเคชันนี้เป็นเว็บแอปพลิเคชันที่พัฒนาขึ้นเพื่อใช้ทำ OCR (Optical Character Recognition) โดยเฉพาะสำหรับการแปลงไฟล์รูปภาพและ PDF ที่มีข้อความภาษาไทย (รวมถึงตัวเลขไทย) และภาษาอังกฤษ ให้เป็นข้อความที่สามารถคัดลอกและนำไปใช้งานต่อได้ง่ายๆ ด้วย User Interface ที่ทันสมัย

## ✨ คุณสมบัติหลัก (Features)
-   **Backend**: Python (Flask) สำหรับประมวลผล OCR
-   **Frontend**: HTML, JavaScript (Fetch API) สำหรับ User Interface
-   **OCR Engine**: ใช้ Tesseract OCR ที่มีความสามารถในการรู้จำข้อความภาษาไทยและอังกฤษได้อย่างแม่นยำ
-   **รองรับไฟล์หลากหลาย**: สามารถประมวลผลไฟล์ประเภท `.pdf`, `.png`, `.jpg`, `.jpeg`, `.bmp`, `.gif`, `.tiff`
-   **รองรับภาษาไทย 100%**: พร้อมด้วยการรู้จำตัวเลขไทย (๐, ๑, ๒, ...) และภาษาอังกฤษอย่างเต็มรูปแบบ
-   **ส่งออกข้อความ**: มีปุ่มสำหรับดาวน์โหลดข้อความที่ได้จาก OCR เป็นไฟล์ `.txt`

## 🛠️ Tech Stack
-   **Backend Framework**: Flask 🐍
-   **Frontend**: HTML5, JavaScript (Vanilla) 🌐
-   **Styling**: Tailwind CSS ✨
-   **Dynamic Background**: particles.js 🌌
-   **Animations**: AOS (Animate On Scroll) 🚀
-   **OCR Core**: Tesseract OCR (with `pytesseract` Python wrapper) 📖
-   **PDF Handling**: Poppler (with `pdf2image` Python wrapper) 📂

## ⚙️ สิ่งที่ต้องติดตั้งก่อน (Prerequisites)

โปรเจกต์นี้ต้องใช้ Tesseract OCR และ Poppler ซึ่งเป็นโปรแกรมที่ต้องติดตั้งแยกในระบบปฏิบัติการของคุณก่อน ไม่สามารถติดตั้งผ่าน `pip` ได้

### สำหรับ macOS (ผ่าน Homebrew) 🍎
หากยังไม่มี Homebrew, [ติดตั้งที่นี่](https://brew.sh/)
จากนั้นรันคำสั่งใน Terminal:
```bash
brew install tesseract tesseract-lang poppler
```
*`tesseract-lang` จะติดตั้งชุดข้อมูลภาษาทั้งหมด รวมถึงภาษาไทย*

### สำหรับ Debian/Ubuntu (Linux) 🐧
รันคำสั่งใน Terminal:
```bash
sudo apt-get update
sudo apt-get install tesseract-ocr tesseract-ocr-tha poppler-utils
```

### สำหรับ Windows 🪟
1.  **Tesseract**: ดาวน์โหลดและติดตั้งจาก [official Tesseract installers](https://github.com/UB-Mannheim/tesseract/wiki)
    -   **สำคัญมาก:** ระหว่างขั้นตอนการติดตั้ง, ให้เลือก "Additional language data" -> "Thai" เพื่อติดตั้งภาษาไทย
    -   หลังติดตั้งเสร็จ ต้องเพิ่ม Path ของ Tesseract ลงใน Environment Variables ของระบบ (เช่น `C:\Program Files\Tesseract-OCR`)
2.  **Poppler**: ดาวน์โหลดไฟล์ zip ของ Poppler [จากที่นี่](https://github.com/oschwartz10612/poppler-windows/releases/)
    -   แตกไฟล์ zip ไปยังตำแหน่งที่ต้องการ (เช่น `C:\poppler`)
    -   เพิ่ม Path ของโฟลเดอร์ `bin` ภายใน Poppler (เช่น `C:\poppler\bin`) ไปยัง Environment Variables ของระบบ

---

## 🚀 ขั้นตอนการติดตั้งโปรเจกต์

1.  **Clone หรือดาวน์โหลดโปรเจกต์นี้**
    ```bash
    # git clone https://github.com/ezynook/Python-OCR.git
    # cd Python-OCR
    ```

2.  **สร้าง Virtual Environment (แนะนำ)**
    เพื่อแยกสภาพแวดล้อมของโปรเจกต์ออกจากระบบหลัก
    ```bash
    python3 -m venv venv
    ```

3.  **เปิดใช้งาน Virtual Environment**
    -   **macOS/Linux:**
        ```bash
        source venv/bin/activate
        ```
    -   **Windows:**
        ```bash
        .\venv\Scripts\activate
        ```

4.  **ติดตั้ง Python packages ที่จำเป็น**
    โปรแกรมจะติดตั้ง Flask, Pytesseract, Pillow, และ pdf2image
    ```bash
    pip install -r requirements.txt
    ```

---

## ▶️ วิธีการรันแอปพลิเคชัน

เมื่อติดตั้งครบทุกขั้นตอนแล้ว ให้รันไฟล์ `app.py` ด้วยคำสั่ง:
```bash
python3 app.py
```
คุณจะเห็นข้อความคล้ายๆ แบบนี้ใน Terminal:
```
 * Serving Flask app 'app'
 * Running on http://127.0.0.1:5000
```

---

## 💡 วิธีการใช้งาน

1.  เปิดเว็บเบราว์เซอร์ (เช่น Chrome, Firefox) แล้วเข้าไปที่ URL:
    [http://127.0.0.1:5000](http://127.0.0.1:5000)

2.  คลิกที่ปุ่ม **"Choose File"** เพื่อเลือกไฟล์ PDF หรือรูปภาพที่ต้องการแปลง

3.  คลิกที่ปุ่ม **"แปลงเป็นข้อความ"**

4.  รอสักครู่ขณะที่ระบบกำลังประมวลผล (จะเห็นข้อความ "กำลังประมวลผล...")

5.  ข้อความที่แปลงได้จะปรากฏในกล่อง "ผลลัพธ์" ด้านล่าง และจะมีปุ่ม **"ส่งออกเป็นข้อความ (.txt)"** ให้ดาวน์โหลดไฟล์

---
Enjoy! 🎉